<form method="post">
    <?= $this->partial('partial/header', ['title' => $this->title, 'file' => $this->file ]) ?>
    <article>
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Id</th>
                    <th>Date</th>
                    <th>Odometer</th>
                    <th>Volume</th>
                    <th>Amount</th>
                    <th>Currency</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->page as $i => $item): ?>
                    <tr>
                        <td><?= sprintf('%02d', $i) ?></td>
                        <td><?= $item->id ?></td>
                        <td><?= $item->date ?></td>
                        <td><?= $item->odometer ?></td>
                        <td><?= $item->volume ?></td>
                        <td><?= $item->amount ?></td>
                        <td><?= $item->currency ?></td>
                        <td class="p-0">
                            <button type="submit" name="action" value="delete,<?= $item->id ?>">delete</button>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </article>
    <footer class="dg c-5">
        <?= $this->paginate($this->page) ?>
        <p class="s-3">&nbsp;</p>
        <a href="<?= $this->url(null, ['action' => 'index'], [], true) ?>"></a>
        <a href="<?= $this->url(null, ['action' => 'file'], [], true) ?>" class="s-2"></a>
    </footer>
</form>
