<?= $this->partial('partial/header', ['prefix' => $this->prefix, 'action' => $this->action, 'file' => $this->file ]) ?>
<?php $this->headMeta()->appendHttpEquiv('refresh', $this->interval . ', URL=' . $this->url()); ?>
<article>
    <div class="dg c-3 tc">
        <div class="dt h-2 bg">
            <div>120s</div>
        </div>
        <div class="dt h-2 bg o-5">
            <div>60s</div>
        </div>
        <div class="dt h-2 bg o-5">
            <div>30s</div>
        </div>
        <table class="s-3">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Time</th>
                    <th>Position</th>
                    <th>Speed</th>
                    <th>Sats</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->page as $item): ?>
                    <tr>
                        <td><?= $item->utc ?></td>
                        <td><?= $item->time ?></td>
                        <td><?= $item->position ?></td>
                        <td><?= $item->speed ?></td>
                        <td><?= $item->sats ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</article>
<footer class="dg c-3">
    <?= $this->paginate($this->page) ?>
    <p>&nbsp;</p>
    <a href="/logger/grid">manage</a>
    <a href="/logger/file">select</a>
</footer>
